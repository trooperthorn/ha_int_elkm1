[mypy]
python_version = 3.14
files = custom_components/elkm1
show_error_codes = true
follow_imports = silent
ignore_missing_imports = true
strict = true
warn_unused_ignores = true

; Upstream elkm1_lib collection stubs expose the Element base class. Targeted
; attr-defined suppressions remain at subclass-only access sites; all other
; strict checks stay enabled and CI requires a zero-error result.
